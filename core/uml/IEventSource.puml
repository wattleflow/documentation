@startuml
' Generated by tools/core_index.py 1.1.0 — do not edit; regenerate.
' Source of truth is the source of wattleflow.core, not this file (D-13).
' Viewpoint: one interface, its inheritance path and its direct core
' descendants. Audience: architect, implementer.

skinparam backgroundColor #FFFFFF
skinparam shadowing false
skinparam classAttributeIconSize 0
hide empty members

title IEventSource — Event-Driven (source role) abstract interface

abstract class ABC <<external>>
class "Generic<Event>" as Generic_Event_ <<external>>

interface IWattleflow <<framework>> {
  +name() : str <<property>>
}

interface "IEventSource<Event>" as IEventSource <<transactional>> #FFF3C4 {
  +register_listener(listener: IEventListener) : None
  +emit_event(event: Event, **kwargs) : None
}

interface IScheduler <<transactional>>

ABC <|-- IWattleflow
IWattleflow <|-- IEventSource
Generic_Event_ <|-- IEventSource
IEventSource <|-- IScheduler : <Event>

legend right
  Subject is highlighted. Ancestors carry their own abstract members,
  so the box stack is the whole contract; descendants are names only — each has its own diagram.
endlegend
@enduml
