@startuml
' Generated by tools/core_index.py 1.1.0 — do not edit; regenerate.
' Source of truth is the source of wattleflow.core, not this file (D-13).
' Viewpoint: one interface, its inheritance path and its direct core
' descendants. Audience: architect, implementer.

skinparam backgroundColor #FFFFFF
skinparam shadowing false
skinparam classAttributeIconSize 0
hide empty members

title IMessageQueue — Message Queue abstract interface (generic bidirectional transport)

abstract class ABC <<external>>
class "Generic<Message, Destination>" as Generic_Message__Destination_ <<external>>

interface IWattleflow <<framework>> {
  +name() : str <<property>>
}

interface "IMessageQueue<Message, Destination>" as IMessageQueue <<concurrent>> #FFF3C4 {
  +send(message: Message, destination: Destination) : None
  +receive(source: Destination, timeout: Optional[float]=None) : Optional[Message]
}

ABC <|-- IWattleflow
IWattleflow <|-- IMessageQueue
Generic_Message__Destination_ <|-- IMessageQueue

legend right
  Subject is highlighted. Ancestors carry their own abstract members,
  so the box stack is the whole contract; no core interface derives from this one.
endlegend
@enduml
